# --- Core web stack (always required) ---
fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
soundfile>=0.12
numpy>=1.24
imageio-ffmpeg>=0.4.9      # ships a portable ffmpeg binary -> decode ANY input format

# --- High quality separation engine (optional but recommended) ---
# Installing these unlocks the real neural separation (Demucs / Hybrid Transformer).
# Without them the app still runs using a built-in DSP "preview" engine.
#
#   pip install -r requirements.txt
#   pip install demucs torch torchaudio
#
# demucs>=4.0.1
# torch>=2.1
# torchaudio>=2.1
